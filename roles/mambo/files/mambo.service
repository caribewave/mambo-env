[Unit]
Description=Hot Board
Requires=docker.service
After=docker.service
[Service]
Restart=always
ExecStart=/usr/local/bin/docker-compose -f /opt/mambo/hot-board.yml up
ExecStop=/usr/local/bin/docker-compose -f /opt/mambo/hot-board.yml down
[Install]
WantedBy=default.target
